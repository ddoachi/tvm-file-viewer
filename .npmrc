# Use local Electron binary (no GitHub download)
# This prevents authentication issues with external GitHub

# Option 1: Use cached Electron
electron_mirror=""

# Option 2: Use local Electron installation
# electron_custom_dir=/path/to/local/electron

# Disable Electron download completely if already installed
ELECTRON_SKIP_BINARY_DOWNLOAD=1
